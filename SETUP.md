# 项目启动指南

## 快速开始

### 1. 安装依赖

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 2. 启动开发服务器

```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev
```

服务器将在 [http://localhost:3000](http://localhost:3000) 启动。

### 3. 构建生产版本

```bash
npm run build
npm start
```

## 功能说明

### 首页
- 展示所有AI工具，按14个分类组织
- 点击**工具名称**：新窗口打开官网
- 点击**Logo**：跳转到工具详情页

### 详情页
- 展示工具的Logo、名称、官网、详细描述
- "Visit Website"按钮：新窗口打开官网
- 显示同类相关工具推荐
- 返回首页按钮

## 项目特点

### 设计特色
- ✨ 像素级还原设计稿
- 🎨 米白色背景 (#e8e6e1)，橙色强调色 (#d97642)
- 🎯 简约的连接线设计
- 🌊 流畅的悬停动效
- 📱 完美适配移动端

### 技术特色
- ⚡ Next.js 14 App Router
- 🎨 Tailwind CSS + shadcn/ui
- 🔍 SEO优化（Sitemap, Robots.txt, OpenGraph）
- 📊 100+ AI工具数据
- 🚫 无滚动条设计
- 🌐 响应式布局

## 数据管理

AI工具数据位于 `/data/ai-tools.ts`。

### 添加新工具

```typescript
{
  id: "tool-id",
  name: "Tool Name",
  url: "https://tool.com",
  description: "工具描述",
  logo: "https://images.unsplash.com/...",
}
```

### 添加新分类

在 `aiToolsData` 数组中添加新的分类对象：

```typescript
{
  id: "category-id",
  name: "CATEGORY NAME",
  color: "text-color-600",
  tools: [...]
}
```

## 文件结构

```
├── app/
│   ├── layout.tsx          # 根布局（包含SEO metadata）
│   ├── page.tsx            # 首页（工具列表）
│   ├── loading.tsx         # 加载状态
│   ├── error.tsx           # 错误页面
│   ├── not-found.tsx       # 404页面
│   ├── sitemap.ts          # 站点地图
│   ├── robots.ts           # 爬虫规则
│   ├── manifest.ts         # PWA配置
│   └── tool/[id]/
│       ├── page.tsx        # 工具详情页
│       └── opengraph-image.tsx  # OG图片
├── components/ui/          # shadcn/ui组件
├── data/ai-tools.ts        # AI工具数据
├── lib/utils.ts            # 工具函数
└── public/                 # 静态资源
```

## 环境变量

创建 `.env.local` 文件：

```bash
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

生产环境需要设置为实际域名。

## 部署

### Vercel（推荐）

```bash
vercel
```

### 其他平台

1. 构建项目：`npm run build`
2. 启动服务：`npm start`
3. 确保设置环境变量 `NEXT_PUBLIC_APP_URL`

## 性能优化

- ✅ 图片使用 Unsplash CDN
- ✅ 无滚动条减少重绘
- ✅ Tailwind CSS 按需加载
- ✅ Next.js 自动代码分割
- ✅ 静态生成 Sitemap 和 Robots

## 浏览器兼容性

- ✅ Chrome (最新)
- ✅ Safari (最新)
- ✅ Firefox (最新)
- ✅ Edge (最新)
- ✅ 移动端浏览器

## 常见问题

### Q: 如何修改配色？
A: 在 `app/globals.css` 中修改 CSS 变量，或直接在组件中修改 Tailwind 类名。

### Q: 如何添加新的AI工具？
A: 编辑 `data/ai-tools.ts` 文件，在对应分类的 `tools` 数组中添加新对象。

### Q: 图片从哪里获取？
A: 当前使用 Unsplash 图片。你可以：
1. 继续使用 Unsplash
2. 替换为实际工具的Logo URL
3. 使用本地图片放在 `public/logos/` 目录

### Q: 如何自定义页面布局？
A: 修改 `app/page.tsx` 和 `app/tool/[id]/page.tsx` 中的 Tailwind 类名。

## License

MIT License

